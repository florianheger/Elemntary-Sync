# Garmin Connect Upload
This file contains all the functionality with the Garmin Connect upload.

## Requirements
1. Be able to authenticate to Garmin Connect and upload files to it.
	1.1. The login happens once with the command "auth-garmin" (email, password and, if enabled, the MFA code). The password is never stored.
	1.2. The resulting tokens are stored in the data volume and refreshed automatically, at least once a day.
2. If failed, wait 30 seconds and retry, at most 5 attempts in total. After that, the upload counts as failed.
	2.1. Errors that retrying can't fix fail immediately: Garmin rejects the file, or the login has expired.
3. If Garmin reports that the activity already exists (HTTP 409), the upload counts as successful.
